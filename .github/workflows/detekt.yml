name: Detekt CI

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7
    - name: set up JDK 21
      uses: actions/setup-java@v5
      with:
        distribution: 'adopt'
        java-version: '21'
        cache: 'gradle'
    - name: Detekt
      run: make detekt
