MAPS_API_KEY=DEFAULT_API_KEY

# Supabase: real values go in secrets.properties (gitignored). These defaults
# keep builds compiling; coffeeShops() falls back to an empty list if unreachable.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_PUBLISHABLE_KEY=
